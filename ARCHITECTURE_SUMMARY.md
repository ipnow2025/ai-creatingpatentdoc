# 아키텍처 재설계 요약

## 📌 핵심 요약

### 프로젝트 종류
- **Next.js 15 (App Router)** 기반 풀스택 애플리케이션
- **AI 기반 특허 명세서 생성 도구**
- **중규모 프로젝트** (확장 가능성 높음)

### 선택한 아키텍처
- **Feature-based Architecture** (주)
- **Layered Architecture** (보조)
- **하이브리드 구조**

---

## 🎯 구조 선택의 이유

### 1. Feature-based Architecture
- ✅ Next.js App Router와 자연스러운 통합
- ✅ 도메인별 완전 독립 모듈 구성
- ✅ 향후 확장 용이 (다른 도메인 추가 시)

### 2. Layered Architecture 요소
- ✅ 비즈니스 로직과 UI 분리
- ✅ API 로직 중앙화
- ✅ 테스트 용이성 향상

### 3. Shared 모듈 분리
- ✅ 공통 코드 재사용성 향상
- ✅ 중복 코드 제거
- ✅ 유지보수성 향상

---

## ✅ 유지보수 시 장점

### 1. 코드 발견성 향상
- 특허 관련 코드 → `features/patent`에서 모두 찾을 수 있음
- 공통 코드 → `shared`에서 찾을 수 있음

### 2. 테스트 용이성
- 각 모듈이 독립적이어서 단위 테스트 작성 용이
- Mock 데이터를 도메인별로 관리 가능

### 3. 확장성
- 새로운 도메인 추가 시 `features/` 하위에 새 폴더만 추가
- 기존 코드에 영향 없음

### 4. 협업 효율성
- 팀원이 서로 다른 도메인을 담당해도 충돌 최소화
- PR 리뷰 시 변경 범위가 명확

### 5. 리팩토링 안정성
- 도메인별로 격리되어 있어 리팩토링 시 영향 범위 제한
- 타입 안정성 향상

---

## ⚠️ 예상 리스크 및 해결책

| 리스크 | 해결책 |
|--------|--------|
| **Import 경로 복잡화** | `tsconfig.json`에 경로 별칭 추가 (`@/features/*`, `@/shared/*`) |
| **순환 의존성** | 의존성 방향 명확히 정의 (shared → features만), ESLint 규칙 설정 |
| **초기 마이그레이션 비용** | 점진적 마이그레이션, 기존 경로 유지하면서 새 구조로 이동 |
| **과도한 추상화** | YAGNI 원칙 적용, 필요할 때만 추가 |
| **Next.js App Router 충돌** | `src/` 폴더 지원 또는 루트에 직접 배치 |

---

## 📁 이상적인 디렉토리 트리 (간략)

```
ai-creating-patent-doc/
├── app/                          # Next.js App Router
│   ├── (patent)/                 # 특허 라우트 그룹
│   │   └── page.tsx
│   ├── api/patent/               # API 라우트 (도메인별 그룹화)
│   │   ├── extract-keywords/
│   │   ├── generate/
│   │   ├── search/
│   │   └── report/
│   └── layout.tsx
│
├── src/
│   ├── features/                 # 기능별 모듈
│   │   └── patent/               # 특허 도메인
│   │       ├── components/      # 도메인 특화 컴포넌트
│   │       ├── hooks/           # 도메인 특화 훅
│   │       ├── services/        # 비즈니스 로직
│   │       └── types/           # 도메인 타입
│   │
│   └── shared/                   # 공통 모듈
│       ├── components/          # 재사용 UI 컴포넌트
│       ├── hooks/               # 공통 훅
│       ├── lib/                 # 유틸리티 및 API 공통 로직
│       └── types/               # 공통 타입
│
└── public/                       # 정적 파일
```

---

## 🔄 파일 이동 가이드 (핵심)

### Phase 1: 공통 모듈
```
기존 → 새로운
app/api/*/route.ts (중복 로직) → src/shared/lib/api/
  - client.ts
  - error-handler.ts
  - api-key-validator.ts

components/main-content.tsx (타입) → src/features/patent/types/
  - patent.types.ts
  - draft.types.ts
  - extraction.types.ts

hooks/use-*.ts (중복) → src/shared/hooks/
```

### Phase 2: 컴포넌트 분리
```
기존 → 새로운
components/main-content.tsx (2000+ 라인) → src/features/patent/components/
  - memo-input/
  - keyword-selector/
  - patent-selector/
  - draft-viewer/
  - patent-detail-modal/

components/sidebar.tsx → src/shared/components/layout/sidebar.tsx
```

### Phase 3: 비즈니스 로직
```
기존 → 새로운
components/main-content.tsx (로직) → src/features/patent/hooks/
  - use-patent-generation.ts
  - use-keyword-extraction.ts
  - use-patent-search.ts
  - use-draft-management.ts

components/main-content.tsx (파싱) → src/features/patent/services/
  - draft-parser.service.ts
```

### Phase 4: API 라우트
```
기존 → 새로운
app/api/extract-keywords/ → app/api/patent/extract-keywords/
app/api/generate-patent/ → app/api/patent/generate/
app/api/search-patents/ → app/api/patent/search/
app/api/generate-report/ → app/api/patent/report/
```

---

## 📋 마이그레이션 체크리스트

### 준비
- [ ] Git 커밋 (백업)
- [ ] 폴더 구조 생성
- [ ] `tsconfig.json` 경로 별칭 추가

### Phase 1: 공통 모듈
- [ ] API 공통 로직 생성
- [ ] 공통 타입 정의
- [ ] 중복 파일 정리

### Phase 2: 컴포넌트
- [ ] 메인 컴포넌트 분리
- [ ] 각 컴포넌트를 독립 폴더로 이동

### Phase 3: 비즈니스 로직
- [ ] 커스텀 훅 생성
- [ ] 서비스 함수 생성

### Phase 4: API 라우트
- [ ] API 라우트를 도메인별로 그룹화
- [ ] 공통 로직 적용

### 검증
- [ ] 빌드 테스트 (`npm run build`)
- [ ] 타입 체크 (`npx tsc --noEmit`)
- [ ] 기능 테스트

---

## 📚 상세 문서

- **전체 분석 및 구조**: `ARCHITECTURE_REDESIGN.md`
- **상세 마이그레이션 가이드**: `MIGRATION_GUIDE.md`
- **이 문서**: `ARCHITECTURE_SUMMARY.md` (요약)

---

## 🎓 핵심 원칙

1. **도메인별로 완전히 독립적인 모듈 구성**
2. **공통 코드는 `shared`에서 중앙 관리**
3. **비즈니스 로직과 UI 분리**
4. **확장성과 유지보수성 최우선**

---

## 💡 다음 단계

1. `ARCHITECTURE_REDESIGN.md` 읽기 (전체 구조 이해)
2. `MIGRATION_GUIDE.md` 따라하기 (단계별 마이그레이션)
3. 각 Phase 완료 후 커밋
4. 빌드 및 테스트 반복

---

**이 구조를 통해 코드의 응집도를 높이고, 모듈 간 결합도를 낮추며, 향후 확장에 대비할 수 있습니다.**

