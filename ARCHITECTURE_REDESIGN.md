# 프로젝트 아키텍처 재설계 문서

## 📊 프로젝트 분석

### 프로젝트 종류
- **프레임워크**: Next.js 15 (App Router)
- **타입**: 풀스택 애플리케이션 (AI 기반 특허 명세서 생성 도구)
- **주요 기술**: React 19, TypeScript, Tailwind CSS, Google Gemini API
- **규모**: 중규모 (현재 단일 도메인, 확장 가능성 높음)

---

## 🔍 현재 구조의 문제점 분석

### 1. **심각도: 높음**

#### 1.1 컴포넌트 구조의 평면화
- `components/main-content.tsx`가 2000+ 라인으로 거대함
- UI 컴포넌트와 비즈니스 로직이 혼재
- 재사용 가능한 컴포넌트가 분리되지 않음

#### 1.2 비즈니스 로직과 UI의 결합
- API 호출 로직이 컴포넌트 내부에 직접 포함
- 상태 관리가 컴포넌트에 집중
- 도메인 로직이 분리되지 않음

#### 1.3 타입 정의의 산재
- 인터페이스가 컴포넌트 파일 내부에 정의됨
- 공통 타입이 재사용되지 않음

### 2. **심각도: 중간**

#### 2.1 API 라우트 구조의 비체계화
- `app/api/` 하위에 기능별 그룹화 없음
- 에러 처리 로직이 각 라우트에 중복
- API 키 검증 로직이 반복됨

#### 2.2 파일 중복
- `hooks/use-mobile.ts`와 `components/ui/use-mobile.tsx` 중복
- `hooks/use-toast.ts`와 `components/ui/use-toast.ts` 중복
- `app/globals.css`와 `styles/globals.css` 중복 가능성

#### 2.3 공통 유틸리티 부재
- API 에러 처리 로직이 각 라우트에 중복
- API 키 검증 로직이 반복
- 응답 파싱 로직이 중복

### 3. **심각도: 낮음**

#### 3.1 스타일 파일 위치 불명확
- `app/globals.css`와 `styles/globals.css` 역할 구분 불명확

#### 3.2 공통 컴포넌트 위치
- `components/sidebar.tsx`와 `components/ui/sidebar.tsx` 혼재 가능성

---

## 🏗️ 이상적인 디렉토리 구조

```
ai-creating-patent-doc/
├── app/                          # Next.js App Router
│   ├── (patent)/                 # 특허 관련 라우트 그룹
│   │   ├── page.tsx              # 메인 페이지
│   │   └── layout.tsx            # 레이아웃 (선택적)
│   ├── api/                      # API 라우트
│   │   └── patent/               # 특허 도메인 API
│   │       ├── extract-keywords/
│   │       │   └── route.ts      # 키워드 추출 API
│   │       ├── generate/
│   │       │   └── route.ts      # 특허 생성 API
│   │       ├── search/
│   │       │   └── route.ts      # 특허 검색 API
│   │       └── report/
│   │           └── route.ts      # 보고서 생성 API
│   ├── globals.css               # 전역 스타일
│   ├── layout.tsx                # 루트 레이아웃
│   └── not-found.tsx             # 404 페이지
│
├── src/                          # 소스 코드 (선택적, 또는 루트에 유지)
│   ├── features/                 # 기능별 모듈 (Feature-based)
│   │   └── patent/               # 특허 도메인
│   │       ├── components/      # 도메인 특화 컴포넌트
│   │       │   ├── memo-input/
│   │       │   │   ├── memo-input.tsx
│   │       │   │   └── memo-input.test.tsx
│   │       │   ├── keyword-selector/
│   │       │   │   ├── keyword-selector.tsx
│   │       │   │   └── keyword-selector.test.tsx
│   │       │   ├── patent-selector/
│   │       │   │   ├── patent-selector.tsx
│   │       │   │   └── patent-selector.test.tsx
│   │       │   ├── draft-viewer/
│   │       │   │   ├── draft-viewer.tsx
│   │       │   │   ├── draft-summary.tsx
│   │       │   │   └── draft-viewer.test.tsx
│   │       │   └── patent-detail-modal/
│   │       │       ├── patent-detail-modal.tsx
│   │       │       └── patent-detail-modal.test.tsx
│   │       ├── hooks/            # 도메인 특화 훅
│   │       │   ├── use-patent-generation.ts
│   │       │   ├── use-keyword-extraction.ts
│   │       │   ├── use-patent-search.ts
│   │       │   └── use-draft-management.ts
│   │       ├── services/         # 비즈니스 로직
│   │       │   ├── patent-api.service.ts
│   │       │   ├── keyword-extraction.service.ts
│   │       │   └── draft-parser.service.ts
│   │       ├── types/           # 도메인 타입
│   │       │   ├── patent.types.ts
│   │       │   ├── draft.types.ts
│   │       │   └── extraction.types.ts
│   │       └── utils/           # 도메인 유틸리티
│   │           └── patent-formatter.ts
│   │
│   ├── shared/                   # 공통 모듈
│   │   ├── components/          # 재사용 가능한 UI 컴포넌트
│   │   │   ├── ui/              # shadcn/ui 컴포넌트 (기존 유지)
│   │   │   │   ├── button.tsx
│   │   │   │   ├── card.tsx
│   │   │   │   └── ...
│   │   │   ├── layout/          # 레이아웃 컴포넌트
│   │   │   │   ├── sidebar.tsx
│   │   │   │   └── header.tsx
│   │   │   └── providers/      # Context Providers
│   │   │       └── theme-provider.tsx
│   │   ├── hooks/              # 공통 훅
│   │   │   ├── use-mobile.ts
│   │   │   └── use-toast.ts
│   │   ├── lib/                # 공통 유틸리티
│   │   │   ├── utils.ts        # cn() 등
│   │   │   ├── api/            # API 공통 로직
│   │   │   │   ├── client.ts   # API 클라이언트
│   │   │   │   ├── error-handler.ts
│   │   │   │   └── api-key-validator.ts
│   │   │   └── constants/      # 상수
│   │   │       └── api.constants.ts
│   │   └── types/              # 공통 타입
│   │       ├── api.types.ts
│   │       └── common.types.ts
│   │
│   └── config/                  # 설정 파일
│       ├── env.ts              # 환경 변수 타입
│       └── api.config.ts       # API 설정
│
├── public/                      # 정적 파일
│   └── ...
│
├── lib/                         # 루트 레벨 유틸리티 (tsconfig 경로용)
│   └── utils.ts                # @/lib/utils 경로 유지
│
├── hooks/                       # 루트 레벨 훅 (tsconfig 경로용)
│   └── ...                     # @/hooks 경로 유지 (선택적)
│
├── types/                       # 루트 레벨 타입 (선택적)
│   └── global.d.ts
│
├── package.json
├── tsconfig.json
├── next.config.mjs
└── tailwind.config.ts
```

---

## 📁 폴더별 역할 설명

### `/app` - Next.js App Router
- **역할**: 라우팅 및 페이지 정의
- **구조**: App Router의 파일 시스템 기반 라우팅 사용
- **특징**: `(patent)` 그룹으로 관련 라우트 묶기

### `/src/features/patent` - 특허 도메인 모듈
- **역할**: 특허 관련 모든 기능을 포함하는 독립 모듈
- **구조**: Feature-based Architecture 패턴
- **장점**: 
  - 기능별로 완전히 독립적
  - 다른 도메인 추가 시 확장 용이
  - 테스트 및 유지보수 용이

#### `/components` - 도메인 특화 컴포넌트
- **역할**: 특허 도메인에만 사용되는 컴포넌트
- **예시**: `memo-input`, `keyword-selector`, `patent-selector`
- **특징**: 각 컴포넌트는 독립 폴더로 관리

#### `/hooks` - 도메인 특화 훅
- **역할**: 특허 도메인의 비즈니스 로직을 캡슐화
- **예시**: `use-patent-generation`, `use-keyword-extraction`
- **특징**: API 호출 및 상태 관리 로직 포함

#### `/services` - 비즈니스 로직
- **역할**: 순수한 비즈니스 로직 (API 호출 제외)
- **예시**: `draft-parser.service.ts` - 특허 초안 파싱 로직
- **특징**: 재사용 가능한 순수 함수

#### `/types` - 도메인 타입
- **역할**: 특허 도메인의 TypeScript 타입 정의
- **예시**: `Patent`, `DraftVersion`, `ExtractedData`
- **특징**: 도메인별로 분리하여 관리

### `/src/shared` - 공통 모듈
- **역할**: 여러 도메인에서 공통으로 사용되는 코드
- **구조**: 
  - `components/`: 재사용 가능한 UI 컴포넌트
  - `hooks/`: 공통 훅
  - `lib/`: 유틸리티 및 공통 로직
  - `types/`: 공통 타입

#### `/shared/lib/api` - API 공통 로직
- **역할**: API 관련 공통 로직 중앙화
- **구성**:
  - `client.ts`: API 클라이언트 (fetch 래퍼)
  - `error-handler.ts`: 에러 처리 로직
  - `api-key-validator.ts`: API 키 검증

### `/public` - 정적 파일
- **역할**: 이미지, 아이콘 등 정적 리소스

---

## 🎯 구조 선택의 이유

### 1. **Feature-based Architecture 채택**
- **이유**: 
  - Next.js App Router와 자연스럽게 통합
  - 도메인별로 완전히 독립적인 모듈 구성
  - 향후 다른 도메인 추가 시 확장 용이 (예: `features/user`, `features/auth`)
- **장점**: 
  - 코드 응집도 향상
  - 모듈 간 결합도 감소
  - 팀 협업 시 충돌 최소화

### 2. **Layered Architecture 요소 결합**
- **이유**: 
  - 비즈니스 로직과 UI 분리 필요
  - API 로직 중앙화 필요
- **구조**:
  - **Presentation Layer**: `components/`
  - **Application Layer**: `hooks/`
  - **Domain Layer**: `services/`
  - **Infrastructure Layer**: `shared/lib/api/`

### 3. **Shared 모듈 분리**
- **이유**: 
  - 공통 코드의 재사용성 향상
  - 중복 코드 제거
  - 유지보수성 향상

---

## ✅ 유지보수 시 장점

### 1. **코드 발견성 향상**
- 특허 관련 코드는 `features/patent`에서 모두 찾을 수 있음
- 공통 코드는 `shared`에서 찾을 수 있음
- 파일 위치가 직관적

### 2. **테스트 용이성**
- 각 모듈이 독립적이어서 단위 테스트 작성 용이
- Mock 데이터를 도메인별로 관리 가능

### 3. **확장성**
- 새로운 도메인 추가 시 `features/` 하위에 새 폴더만 추가
- 기존 코드에 영향 없음

### 4. **협업 효율성**
- 팀원이 서로 다른 도메인을 담당해도 충돌 최소화
- PR 리뷰 시 변경 범위가 명확

### 5. **리팩토링 안정성**
- 도메인별로 격리되어 있어 리팩토링 시 영향 범위 제한
- 타입 안정성 향상

---

## ⚠️ 예상 리스크 및 해결책

### 1. **리스크: Import 경로 복잡화**
- **문제**: `@/features/patent/components/...` 같은 긴 경로
- **해결책**: 
  - `tsconfig.json`에 경로 별칭 추가
  ```json
  {
    "paths": {
      "@/*": ["./*"],
      "@/features/*": ["./src/features/*"],
      "@/shared/*": ["./src/shared/*"]
    }
  }
  ```
  - 또는 각 feature에 `index.ts`로 re-export

### 2. **리스크: 순환 의존성**
- **문제**: 모듈 간 순환 참조 발생 가능
- **해결책**:
  - 의존성 방향 명확히 정의 (shared → features 방향만)
  - ESLint 규칙으로 순환 의존성 감지
  - `shared`는 `features`를 import하지 않도록 규칙 설정

### 3. **리스크: 초기 마이그레이션 비용**
- **문제**: 기존 코드를 새 구조로 이동하는 작업
- **해결책**:
  - 점진적 마이그레이션 (한 번에 모두 이동하지 않음)
  - 기존 경로를 유지하면서 새 구조로 점진적 이동
  - 파일 이동 가이드 제공 (아래 참조)

### 4. **리스크: 과도한 추상화**
- **문제**: 작은 프로젝트에 과도한 구조 적용
- **해결책**:
  - 현재는 중규모이므로 적절한 수준
  - 필요 시 `services/` 폴더는 생략하고 `hooks/`에 통합 가능
  - YAGNI 원칙 적용 (필요할 때만 추가)

### 5. **리스크: Next.js App Router와의 충돌**
- **문제**: `src/` 폴더 사용 시 경로 설정 복잡
- **해결책**:
  - Next.js는 `src/` 폴더를 지원하므로 문제 없음
  - 또는 루트에 `features/`, `shared/` 직접 배치 (권장)
  - `tsconfig.json` 경로만 올바르게 설정

---

## 📋 파일 이동 가이드

### Phase 1: 공통 모듈 정리

#### 1.1 API 공통 로직 생성
```
기존: app/api/*/route.ts (각 파일에 중복된 에러 처리)
→ 
새로운: src/shared/lib/api/
  - client.ts (새로 생성)
  - error-handler.ts (새로 생성)
  - api-key-validator.ts (새로 생성)
```

**작업 내용**:
1. `src/shared/lib/api/api-key-validator.ts` 생성
   - API 키 검증 로직 추출
2. `src/shared/lib/api/error-handler.ts` 생성
   - 공통 에러 처리 로직 추출
3. `src/shared/lib/api/client.ts` 생성
   - Gemini API 호출 래퍼 함수

#### 1.2 공통 타입 정의
```
기존: components/main-content.tsx (인터페이스가 파일 내부)
→
새로운: src/features/patent/types/
  - patent.types.ts
  - draft.types.ts
  - extraction.types.ts
```

**작업 내용**:
1. `src/features/patent/types/patent.types.ts` 생성
   - `Patent`, `PatentDetail` 타입 이동
2. `src/features/patent/types/draft.types.ts` 생성
   - `DraftVersion` 타입 이동
3. `src/features/patent/types/extraction.types.ts` 생성
   - `ExtractedData` 타입 이동

#### 1.3 중복 파일 정리
```
기존: 
  - hooks/use-mobile.ts
  - components/ui/use-mobile.tsx
  - hooks/use-toast.ts
  - components/ui/use-toast.ts
→
새로운: src/shared/hooks/
  - use-mobile.ts (하나만 유지)
  - use-toast.ts (하나만 유지)
```

**작업 내용**:
1. 중복 파일 확인 및 통합
2. `src/shared/hooks/`로 이동
3. 모든 import 경로 업데이트

### Phase 2: 컴포넌트 분리

#### 2.1 메인 컴포넌트 분해
```
기존: components/main-content.tsx (2000+ 라인)
→
새로운: src/features/patent/components/
  - memo-input/
    - memo-input.tsx
  - keyword-selector/
    - keyword-selector.tsx
  - patent-selector/
    - patent-selector.tsx
  - draft-viewer/
    - draft-viewer.tsx
    - draft-summary.tsx
  - patent-detail-modal/
    - patent-detail-modal.tsx
```

**작업 내용**:
1. `memo-input.tsx` 생성
   - 메모 입력 섹션 추출 (라인 1095-1204)
2. `keyword-selector.tsx` 생성
   - 키워드 선택 섹션 추출 (라인 1206-1388)
3. `patent-selector.tsx` 생성
   - 특허 선택 섹션 추출 (라인 1390-1543)
4. `draft-viewer.tsx` 생성
   - 초안 뷰어 섹션 추출 (라인 1546-1783)
5. `patent-detail-modal.tsx` 생성
   - 특허 상세 모달 추출 (라인 1787-2011)

#### 2.2 레이아웃 컴포넌트 이동
```
기존: components/sidebar.tsx
→
새로운: src/shared/components/layout/sidebar.tsx
```

### Phase 3: 비즈니스 로직 분리

#### 3.1 훅 생성
```
기존: components/main-content.tsx (상태 관리 및 API 호출 로직)
→
새로운: src/features/patent/hooks/
  - use-patent-generation.ts
  - use-keyword-extraction.ts
  - use-patent-search.ts
  - use-draft-management.ts
```

**작업 내용**:
1. `use-keyword-extraction.ts` 생성
   - 키워드 추출 로직 추출 (handleMemoSubmit 함수)
2. `use-patent-search.ts` 생성
   - 특허 검색 로직 추출 (handleSelectInfoAndSearch, handleSearchAndGenerateDraft3)
3. `use-patent-generation.ts` 생성
   - 특허 생성 로직 추출 (handleGenerateDraft1, handleGenerateDraft2, handleGenerateDraft3, handleRefineDraft)
4. `use-draft-management.ts` 생성
   - 초안 관리 로직 추출 (draftVersions 상태 관리)

#### 3.2 서비스 생성
```
기존: components/main-content.tsx (파싱 로직)
→
새로운: src/features/patent/services/
  - draft-parser.service.ts
```

**작업 내용**:
1. `draft-parser.service.ts` 생성
   - `parseDraftSections`, `parseStructuredSummary`, `cleanContent` 함수 이동

### Phase 4: API 라우트 리팩토링

#### 4.1 API 라우트 구조 변경
```
기존: app/api/
  - extract-keywords/route.ts
  - generate-patent/route.ts
  - search-patents/route.ts
  - generate-report/route.ts
→
새로운: app/api/patent/
  - extract-keywords/route.ts
  - generate/route.ts (generate-patent → generate)
  - search/route.ts (search-patents → search)
  - report/route.ts (generate-report → report)
```

**작업 내용**:
1. 각 라우트에서 공통 로직을 `shared/lib/api/`로 이동
2. API 키 검증을 `api-key-validator.ts` 사용
3. 에러 처리를 `error-handler.ts` 사용

### Phase 5: 스타일 정리

#### 5.1 스타일 파일 통합
```
기존: 
  - app/globals.css
  - styles/globals.css (중복 가능성)
→
새로운: app/globals.css (하나만 유지)
```

**작업 내용**:
1. 중복 확인 및 통합
2. `styles/globals.css` 삭제 (사용되지 않는 경우)

---

## 🔄 마이그레이션 체크리스트

### Step 1: 준비 작업
- [ ] `src/` 폴더 구조 생성
- [ ] `tsconfig.json` 경로 별칭 추가
- [ ] 공통 타입 파일 생성

### Step 2: 공통 모듈 이동
- [ ] API 공통 로직 추출 및 이동
- [ ] 공통 훅 통합 및 이동
- [ ] 공통 타입 정의

### Step 3: 컴포넌트 분리
- [ ] 메인 컴포넌트를 작은 컴포넌트로 분리
- [ ] 각 컴포넌트를 독립 폴더로 이동
- [ ] Props 타입 정의

### Step 4: 비즈니스 로직 분리
- [ ] 커스텀 훅 생성 및 로직 이동
- [ ] 서비스 함수 생성 및 이동
- [ ] 상태 관리 로직 정리

### Step 5: API 라우트 리팩토링
- [ ] API 라우트를 도메인별로 그룹화
- [ ] 공통 로직 적용
- [ ] 에러 처리 통일

### Step 6: 테스트 및 검증
- [ ] 모든 import 경로 확인
- [ ] 빌드 테스트
- [ ] 기능 동작 확인
- [ ] 타입 에러 확인

---

## 📝 추가 권장사항

### 1. **환경 변수 관리**
- `src/config/env.ts`에서 환경 변수 타입 정의
- `process.env` 직접 사용 대신 타입 안전한 접근

### 2. **에러 바운더리**
- `src/shared/components/error-boundary.tsx` 생성
- 전역 에러 처리

### 3. **로딩 상태 관리**
- `src/shared/components/loading.tsx` 생성
- 공통 로딩 UI

### 4. **테스트 설정**
- 각 컴포넌트 폴더에 `*.test.tsx` 파일 추가
- Jest/Vitest 설정

### 5. **문서화**
- 각 feature에 `README.md` 추가
- 주요 함수에 JSDoc 주석

---

## 🎓 결론

이 구조는 **Feature-based Architecture**를 기반으로 하되, **Layered Architecture**의 장점을 결합한 하이브리드 구조입니다. 

**핵심 원칙**:
1. 도메인별로 완전히 독립적인 모듈 구성
2. 공통 코드는 `shared`에서 중앙 관리
3. 비즈니스 로직과 UI 분리
4. 확장성과 유지보수성 최우선

이 구조를 통해 코드의 응집도를 높이고, 모듈 간 결합도를 낮추며, 향후 확장에 대비할 수 있습니다.

